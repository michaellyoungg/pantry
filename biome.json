{
  "$schema": "https://biomejs.dev/schemas/2.5.3/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignoreUnknown": true,
    "includes": [
      "**",
      "!**/.claude",
      "!**/_generated",
      "!**/dist",
      "!**/node_modules",
      "!**/.turbo",
      "!**/public",
      "!**/*.css",
      "!**/routeTree.gen.ts",
      "!pnpm-lock.yaml"
    ]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "style": {
        "noNonNullAssertion": "warn"
      },
      "suspicious": {
        "noArrayIndexKey": "warn"
      },
      "correctness": {
        "useExhaustiveDependencies": "warn"
      }
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "double"
    }
  },
  "overrides": [
    {
      "includes": ["**/packages/core/src/**", "!**/packages/core/src/**/*.test.ts"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedGlobals": {
              "level": "error",
              "options": {
                "deniedGlobals": {
                  "window": "packages/core must stay platform-free — take the value as an argument instead.",
                  "document": "packages/core must stay platform-free — no DOM access.",
                  "navigator": "packages/core must stay platform-free — no DOM access.",
                  "localStorage": "packages/core must stay platform-free — pass storage in from the client.",
                  "sessionStorage": "packages/core must stay platform-free — pass storage in from the client.",
                  "alert": "packages/core must stay platform-free — return the message and let the client show it.",
                  "confirm": "packages/core must stay platform-free — return the message and let the client show it.",
                  "prompt": "packages/core must stay platform-free — return the message and let the client show it."
                }
              }
            },
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": ["react-dom", "react-dom/**", "**/*.css"],
                    "message": "packages/core is headless: no renderer and no styling."
                  }
                ]
              }
            }
          }
        }
      }
    },
    {
      "includes": [
        "**/packages/core/src/**",
        "!**/packages/core/src/react/**",
        "!**/packages/core/src/data/**",
        "!**/packages/core/src/**/*.test.ts"
      ],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "react": "Only packages/core/src/react and src/data (the @pantry/core/react and @pantry/core/data entry points) may import React."
                },
                "patterns": [
                  {
                    "group": ["react-dom", "react-dom/**", "**/*.css"],
                    "message": "packages/core is headless: no renderer and no styling."
                  }
                ]
              }
            }
          }
        }
      }
    }
  ],
  "assist": {
    "enabled": true,
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  }
}
