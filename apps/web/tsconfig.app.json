{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023", "DOM"],
    "module": "esnext",
    // "node" is required because the @pantry/convex/api type chain reaches
    // Convex action source (process.env). Footgun: app code can reference Node
    // globals that don't exist in the browser. Remove once @pantry/convex ships
    // a browser-safe declaration entry (see backlog BL-0011).
    "types": ["vite/client", "node"],
    "paths": {
      "@pantry/convex/api": ["../../packages/convex/convex/_generated/api.d.ts"]
    },
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
